<Window x:Class="TaflWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaflWPF"
        xmlns:converter="clr-namespace:TaflWPF.Converter"
        mc:Ignorable="d"
        Title="Tafl Dekstop" MinWidth="320" MinHeight="200" Width="600" Height="450">
    <Window.Resources>
        <converter:PieceToImageConverter x:Key="PieceConverter"/>
        <converter:PositionToImageConverter x:Key="PositionConverter"/>
    </Window.Resources>
    <DockPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Viewbox StretchDirection="Both" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0">
                <UniformGrid Margin="10,10,10,10">
                    <ItemsControl ItemsSource="{Binding BoardVM.Pieces}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="{Binding BoardVM.RowCount}" Columns="{Binding BoardVM.ColumnCount}"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="GridBorder" BorderThickness="1" BorderBrush="Black">
                                    <Grid>
                                        <Image RenderOptions.BitmapScalingMode="HighQuality"
                                            MinWidth="16" MinHeight="16" MaxWidth="32" MaxHeight="32">
                                            <Image.Source>
                                                <MultiBinding Converter="{StaticResource PositionConverter}">
                                                    <Binding ElementName="GridBorder"/>
                                                </MultiBinding>
                                            </Image.Source>
                                        </Image>
                                    <Image Source="{Binding Converter={StaticResource PieceConverter}}"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       MinWidth="16" MinHeight="16" MaxWidth="32" MaxHeight="32" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </UniformGrid>
            </Viewbox>
        </Grid>
        <StackPanel Margin="10" Grid.Column="1" Grid.Row="0">
            <TextBlock Text="Enter move: " Margin="5" DockPanel.Dock="Top" />
            <TextBox Padding="2" Margin="5" DockPanel.Dock="Top" />
        </StackPanel>
    </DockPanel>
</Window>
